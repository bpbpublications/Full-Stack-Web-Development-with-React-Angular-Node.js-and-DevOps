# Mini Project — Modern Landing Page (Advanced CSS)

## Features used
- CSS variables + theme-ready palette
- Hero background overlay with image
- Glass cards, gradients, shadows
- Responsive layout (Grid + Flex)
- Buttons with hover transitions
- Small animation for a "pulse" badge
- clamp() for responsive type

## Run
Open `index.html` in a browser.
