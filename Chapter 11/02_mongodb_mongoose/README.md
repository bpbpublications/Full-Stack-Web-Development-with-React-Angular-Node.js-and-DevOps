02_mongodb_mongoose – MongoDB Integration with Mongoose

This folder contains:
- db.js        : connectDB() helper
- user.model.js: Mongoose schema/model example
- app_mongoose.js: Express app exposing basic CRUD endpoints

Install dependencies:
  npm install express mongoose dotenv
